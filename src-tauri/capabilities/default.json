{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs plus the native dialog used by the Rust-side folder picker.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
